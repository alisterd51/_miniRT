Pour tester le rendeur 3d:
-compiler avec:
	make all
-tester avec:
	./miniRT scenes/all.rt
-tester la sauvegarde avec:
	./miniRT scenes/all.rt --save

key mapping:
	translation x: LEFT_KEY, RIGHT_KEY
	translation y: S_KEY, Z_KEY
	translation z: UP_KEY, DOWN_KEY
	rotation x: K_KEY, L_KEY
	rotation y: H_KEY, J_KEY
	rotation z: Q_KEY, D_KEY
	changement cam: PAGE_UP, PAGE_DOWN
	rendu: R_KEY
	save and exit: P_KEY
	exit: ESC_KEY

bonus:
	Compound element: Cube
	Anti-aliasing
	Multithreaded rendering
	Keyboard interactivity (translation/rotation) with camera

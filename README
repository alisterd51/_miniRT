Pour tester le rendeur 3d:
-compiler avec:
	make debug
-tester avec:
	./test_parseur.out scenes/test.rt
-tester la sauvegarde avec:
	./test_parseur.out scenes/test.rt --save

nota:
	transp non fonctionnel (bonus)
	cylindre non fonctionnel
	partie ombre des objets plats non gere
	lumiere colore non fonctionnel
	taille auto non gere
	test taille image
	test declaration unique maj
	scene a faire pur chaque cas
	initialisation angle camera non teste
	cube (bonus)
	pyramide (bonus)
	cone (bonus)
	filtre couleur (bonus)
	stereoscopie (bonus)
	interaction clavier objet (bonus)
	rotation cam souris (bonus)

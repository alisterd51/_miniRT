Pour tester le parseur:
-compiler avec:
	make miniRT && make debug
-tester avec (ou n'importe quelle .rt):
	./test_parseur.out scenes/legend.rt

Pour tester le rendeur 3d:
-compiler avec:
	make miniRT
-tester avec:
	./miniRT scenes/test.rt
-tester la sauvegarde avec:
	./miniRT scenes/test.rt --save

nota:
	mirroir et transp non fonctionnel
	cylindre non fonctionnel
	rotation cam non fonctionnel
